# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{Jestfer/database-server-ruby}" }

gem 'sinatra'

group :development, :test do
  gem 'capybara'
  gem 'rspec'
end
